import { SearchcoachesPipe } from './searchcoaches.pipe';

describe('SearchcoachesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcoachesPipe();
    expect(pipe).toBeTruthy();
  });
});
